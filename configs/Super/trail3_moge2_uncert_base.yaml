# SemSup trail_3 (mogev2) ARM-2 A/B CONTROL (uncertainty OFF). Matched to trail3_moge2_uncert.yaml
# -> bit-identical to the plain trail3_moge2 base (head never constructed when enable:false).
inherit_from: configs/Super/trail3_moge2.yaml
data:
  output: output/trail3_moge2_uncert_base
  exp_name: demo
uncertainty:
  enable: false
