# Battery-7 REDESIGN re-test (seed 0). The stabiliser (reg 0.003 / lr_mult 0.1) PLUS the redesign:
#  - seed fixed (ddsslam now CALLS seed_everything; was dead code -> the battery-6 coin-flip),
#  - deform_surface_bind: deformation restricted to within ~2*trunc of the measured surface
#    (kills the off-surface field explosion that inflated battery-6 raw 108 -> tanh saturation).
# Run s0/s1/s2 (different seeds): if the field is LIVE + bounded + consistent ACROSS seeds, the redesign
# fixed the reproducibility failure (battery-5 LIVE 0.12 vs battery-6 DEAD 5.9e-9 was a seed coin-flip).
inherit_from: configs/Super/pb5_reg0p003_lrslow.yaml
deform_surface_bind: 2.0
seed: 0
data:
  output: output/pb7_redesign_s0
  exp_name: demo
